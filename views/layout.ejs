<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.0/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-800 font-sans">
    <header class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">Online Library</h1>
            <nav>
                <a href="/products" class="text-white mx-2 hover:underline">Books</a>
                <a href="/categories" class="text-white mx-2 hover:underline">Categories</a>
                <a href="/account" class="text-white mx-2 hover:underline">My Account</a>
                <% if (typeof user !== 'undefined' && user) { %>
                    <span class="text-white mx-2">Welcome, <%= user.email %>!</span> <!-- Affiche l'email de l'utilisateur -->
                    <a href="/auth/logout" class="text-white mx-2 hover:underline">Logout</a>
                <% } else { %>
                    <a href="/auth/login" class="text-white mx-2 hover:underline">Login</a>
                <% } %>
            </nav>
        </div>
    </header>
    <main class="container mx-auto my-8 p-4 bg-white rounded-lg shadow">
        <%- include(body) %>
    </main>
    <footer class="fixed w-full bottom-0 bg-gray-800 text-white p-4 mt-8">
        <div class="container mx-auto text-center">
            <p>© 2024 Online Library. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
